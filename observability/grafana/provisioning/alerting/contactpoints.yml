apiVersion: 1

contactPoints:
  - orgId: 1
    name: slack-alerts
    receivers:
      - uid: slack-webhook
        type: slack
        settings:
          url: ${SLACK_WEBHOOK_URL}
          title: "Lumio Alert: {{ .CommonLabels.alertname }}"
          text: "{{ .CommonAnnotations.summary }}"
